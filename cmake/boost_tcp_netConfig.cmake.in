@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(Boost REQUIRED COMPONENTS system)
find_dependency(Threads REQUIRED)

if(@BNET_WITH_ZLIB@)
    find_dependency(ZLIB REQUIRED)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/boost_tcp_netTargets.cmake")
